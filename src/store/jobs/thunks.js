import { createAsyncThunk } from "@reduxjs/toolkit";

export const addJob = createAsyncThunk(
    'job/add',
    async(user, thunkAPI) => {
        try {
            
        } catch (error) {
            
        }
    }    
)